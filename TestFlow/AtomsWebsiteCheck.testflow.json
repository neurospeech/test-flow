{
    "label": "Atoms Site Test",
    "config": {
        "debug": 1
    },
    "steps": [
        [ "navigate", "http://atoms.azurewebsites.net" ],
        [ "assert", "window.$", "jQuery loaded" ],
        [ "assert" , "window.$('img:last-of-type').attr('alt') == 'Share on Facebook'", "loaded success"]
    ]
}